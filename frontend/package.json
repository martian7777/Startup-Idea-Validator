{
  "name": "frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint"
  },
  "dependencies": {
    "next": "16.2.12",
    "react": "19.2.4",
    "react-dom": "19.2.4",
    "recharts": "^3.10.1"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.2.12",
    "tailwindcss": "^4",
    "typescript": "^5"
  },
  "//": "overrides: next@16.2.12 (currently `latest`) pins postcss to an exact 8.4.31 and sharp to ^0.34.5, both under high-severity advisories. No stable next release fixes this yet -- even 16.3.0-preview.10 pins postcss 8.5.10, still inside the vulnerable <=8.5.17 range. Remove these once a stable next ships patched pins.",
  "overrides": {
    "postcss": "^8.5.25",
    "sharp": "^0.35.3"
  }
}
